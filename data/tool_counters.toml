# Tool Usage Counters — INTENTIONALLY DISABLED 2026-05-20
#
# The hardcoded counters that used to live here were honesty-incompatible:
# static numbers shown as if they were live usage data. The monthly GA4
# refresh pipeline (.github/workflows/refresh-counters.yml +
# scripts/refresh-counters.py) was also silently broken since at least
# 2026-05-01 because of a wrong GA4 property ID hardcoded as default,
# so the numbers were stale on top of being decorative.
#
# Partials (layouts/partials/tool-header.html + layouts/partials/tool-hero.html)
# guard the counter render with {{ if gt $counterBase 0 }}, so an empty
# tools array = no counter renders anywhere. Safe and reversible.
#
# What is UNAFFECTED by this disablement:
#   - Live "X reading now" pill in nav + tool headers (separate code path,
#     fed by /api/stats?realtime=1 via Cloudflare KV cache + GHA cron)
#   - Blog post reads counter in layouts/_default/single.html (independent
#     inline formula, not driven by this file)
#   - Freshness badge ("Updated X ago") on pipeline tools
#
# To re-enable in future, repopulate [[tools]] entries here and also
# re-enable the refresh-counters.yml workflow.

tools = []
