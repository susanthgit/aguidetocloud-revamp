{
  "name": "brainbar-mcp",
  "version": "0.1.0",
  "description": "Brain Bar — Microsoft jargon decoder, exposed as a remote MCP server for AI agents.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "esbuild": "^0.28.0",
    "typescript": "^5",
    "wrangler": "^3"
  }
}
